# Специфікація тестових програм для верифікації транслятора

Цей документ містить опис алгоритмів для перевірки коректності роботи розробленого транслятора. 

> **Важливо:** При генерації коду необхідно використовувати лексику та синтаксис згідно з індивідуальним завданням (ключові слова, символи операцій, правила іменування ідентифікаторів).

---

## 1. Тестова програма «Лінійний алгоритм»
**Назва програми в коді:** `LinearAlgorithm` (у коментарі на початку)

### Кроки виконання:
1. **Введення:** Отримати значення для двох змінних (наприклад, `varA`, `varB`).
2. **Арифметичний вивід:** Вивести на екран результати наступних операцій:
   - Додавання: `varA + varB`
   - Віднімання: `varA - varB`
   - Множення: `varA * varB`
   - Ділення: `varA / varB`
   - Залишок від ділення: `varA % varB`
3. **Обчислення виразів:**
   - `X = (varA - varB) * 10 + (varA + varB) / 10`
   - `Y = X + (X % 10)`
4. **Виведення:** Надрукувати кінцеві значення `X` та `Y`.

---

## 2. Тестова програма «Алгоритм з розгалуженням»
**Назва програми в коді:** `BranchingAlgorithm` (у коментарі на початку)

### Кроки виконання:
1. **Введення:** Три числа (наприклад, `A`, `B`, `C`).
2. **Вкладений умовний оператор:**
   - Знайти максимальне число серед `A`, `B`, `C`.
   - Вивести результат на екран.
3. **Логічне "І" (AND):**
   - Умова: `(A = B) AND (A = C) AND (B = C)`
   - Якщо істина — вивести `1`, інакше — `0`.
4. **Логічне "АБО" (OR):**
   - Умова: `(A < 0) OR (B < 0) OR (C < 0)`
   - Якщо істина — вивести `-1`, інакше — `0`.
5. **Логічне заперечення (NOT):**
   - Умова: `!(A < (B + C))`
   - Якщо істина — вивести `10`, інакше — `0`.

---

## 3. Тестова програма «Циклічний алгоритм»
**Назва програми в коді:** `CyclicAlgorithm` (у коментарі на початку)

### Кроки виконання:
1. **Введення:** Два числа `A` та `B` (за умови `A < B`).
2. **Простий цикл:**
   - Пройти циклом від `A` до `B` включно.
   - На кожній ітерації вивести квадрат поточного числа.
3. **Вкладений цикл (Обчислення X = A * B):**
   - Встановити `X = 0`.
   - Зовнішній цикл: від `1` до `A` (крок 1).
   - Внутрішній цикл: від `1` до `B` (крок 1).
   - Тіло внутрішнього циклу: `X = X + 1`.
4. **Виведення:** Надрукувати значення `X`.

---

## Технічні зауваження для Claude Code
- Всі ідентифікатори мають відповідати регулярному виразу ідентифікатора з вашої граматики.
- Оператори порівняння (`=`, `!=`, `<`, `>`, `<=`, `>=`) та логічні зв'язки повинні відповідати вашій специфікації.
- Коментарі мають бути оформлені згідно з правилами вашої мови.
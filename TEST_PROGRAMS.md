# Тестові програми для транслятора Z07

**Автор:** Запливаний Данііл Валерійович
**Варіант:** 07
**Розширення файлів:** `.z07`

## Структура тестів

Усі тестові програми написані на мові Z07 та розташовані у файлах з розширенням `.z07`:

1. `test1_linear.z07` - Лінійний алгоритм
2. `test2_branch.z07` - Алгоритм з розгалуженням
3. `test3_loop.z07` - Циклічний алгоритм

## Запуск тестів

### Компіляція транслятора:
```bash
g++ -o lab_4_5 main.cpp lexer.cpp parser.cpp semantic.cpp codegen.cpp
```

### Запуск тестових програм:
```bash
./lab_4_5 test1_linear.z07
./lab_4_5 test2_branch.z07
./lab_4_5 test3_loop.z07
```

### Запуск згенерованого коду:
Після трансляції створюється файл `output.c` та виконуваний файл `./output`

## Тест 1: Лінійний алгоритм (`test1_linear.z07`)

### Опис:
1. Ввести два числа A і B
2. Вивести результати операцій: A+B, A-B, A*B, A/B, A%B
3. Обрахувати: X = (A-B)*10 + (A+B)/10
4. Обрахувати: Y = X + X%10
5. Вивести X і Y

### Приклад виконання:
```
Ввід: A=15, B=5

Вивід:
20    (A + B)
10    (A - B)
75    (A * B)
3     (A / B)
0     (A % B)
102   (X)
104   (Y)
```

### Перевірка:
- X = (15-5)*10 + (15+5)/10 = 100 + 2 = 102 ✓
- Y = 102 + 102%10 = 102 + 2 = 104 ✓

---

## Тест 2: Алгоритм з розгалуженням (`test2_branch.z07`)

### Опис:
1. Ввести три числа A, B, C
2. **Вкладений if:** Знайти максимальне значення
3. **Простий if:** Перевірити чи всі числа однакові (вивести 1 або 0)
4. **Простий if:** Перевірити чи є від'ємні (вивести -1 або 0)
5. **Простий if:** Перевірити чи A > B+C (вивести 10 або 0)

### Приклад виконання:
```
Ввід: A=25, B=15, C=30

Вивід:
30    (максимальне значення)
0     (не всі однакові)
0     (немає від'ємних)
0     (25 не більше за 15+30)
```

### Перевірка:
- MAX(25, 15, 30) = 30 ✓
- 25=15 і 25=30 і 15=30? НІ → 0 ✓
- всі >= 0? ТАК → 0 ✓
- 25 >= 45? НІ → 0 ✓

---

## Тест 3: Циклічний алгоритм (`test3_loop.z07`)

### Опис:
1. Ввести два числа A і B (A < B)
2. **Простий цикл:** Вивести квадрати чисел від A до B
3. **Вкладений цикл:** Обрахувати X=A*B методом додавання
4. Вивести X

### Приклад виконання:
```
Ввід: A=3, B=5

Вивід:
9     (3²)
16    (4²)
25    (5²)
15    (3*5 через вкладені цикли)
```

### Перевірка:
- Квадрати: 3²=9, 4²=16, 5²=25 ✓
- X = 3*5 = 15 (через 3 ітерації по 5 інкрементів) ✓

---

## Особливості реалізації

### Обмеження мови Z07:
1. **Ідентифікатори:** всі літери ВЕЛИКІ, макс 4 символи (Up4)
2. **Тип даних:** int16_t (діапазон 0-32767)
3. **Оператори:** `<-` (присвоєння), `get`, `put`, `if-else`, `for-in`
4. **Логічні оператори:** `eg`, `ne`, `>=`, `<=`, `and`, `or`, `!`
5. **Коментарі:** `%% текст %%`

### Труднощі:
- Відсутність оператора `<` та `>` (тільки `>=` та `<=`)
- Змінні мають бути короткими (4 символи), тому використовуються TMP, RES тощо
- Не підтримуються від'ємні числа безпосередньо (тільки через віднімання)

---

## Результати тестування

| Тест | Компіляція | Виконання | Коректність |
|------|------------|-----------|-------------|
| Тест 1 (Лінійний) | ✅ OK | ✅ OK | ✅ Verified |
| Тест 2 (Розгалуження) | ✅ OK | ✅ OK | ✅ Verified |
| Тест 3 (Цикли) | ✅ OK | ✅ OK | ✅ Verified |

Усі три тестові програми успішно проходять:
- Лексичний аналіз
- Синтаксичний аналіз
- Семантичний аналіз
- Генерацію C коду
- Компіляцію gcc
- Виконання з коректними результатами
